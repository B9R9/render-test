{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "exercises_3.1-3.6",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../front-end && npm run build && cp -r build ../backend",
    "deploy:full": "npm run build:ui && cd .. && git add . && git commit -m uibuild && git push"
  },
  "author": "baptiste",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
